import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "crmapp Privacy Policy",
};

export default function PrivacyPolicyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
